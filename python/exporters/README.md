# Python Exporters

Exporter experiments, including Splunk export work, remain in Python for now.
Exporters should map normalized OpenAssetWatch records without forcing
downstream-specific naming into the core schema.
