# Python Advisor

AI advisor workflows remain in Python for now. Advisor code should consume
normalized assets, findings, and evidence artifacts. It must not execute tools,
run scanners, mutate infrastructure, or expose secrets.
