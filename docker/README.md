# Docker

Reserved for Docker packaging and deployment helpers. The root
`docker-compose.yml` remains the current local development stack; installer
compose scaffolding lives under `installers/docker/`.
