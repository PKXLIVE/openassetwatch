# Frontend Placeholder

This directory is reserved for future OpenAssetWatch frontend experiments.

The current Control Tower MVP dashboard is intentionally static and lives in
`backend/app/static/`. The local `web/` container serves those static assets for
Docker Compose development.
