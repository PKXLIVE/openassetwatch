# OpenAssetWatch Agent tar.gz Fallback Source

The tar.gz package is a manual fallback for unsupported Linux distributions.
It contains the portable agent binary and release metadata only. It does not
create users or groups, install systemd units, modify sudoers, run package
managers, or overwrite administrator-managed config or identity files.
